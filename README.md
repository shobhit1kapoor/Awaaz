# AI Buddy Windows

Tauri 2 Windows port scaffold for Clicky. The original Swift source is copied into `clicky-reference/leanring-buddy/` and should be treated as read-only behavior reference.

## Setup

```bash
npm install
cp .env.example .env
npm run tauri dev
```

Worker setup lives in `worker/`.
